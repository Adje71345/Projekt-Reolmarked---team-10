<UserControl x:Class="Reolmarked.View.TopbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WhiteBrush}"
             Height="40">

    <Border Background="{DynamicResource WhiteBrush}"
            Padding="0"
            CornerRadius="10,10,10,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Titel -->
            <TextBlock Text="Medarbejdersite"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource HeaderBrush}"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0" />



            <!-- Knapper -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0">
                <Button Content="Hjælp"
                        Style="{StaticResource RoundedButton}" />
                <Button Content="{Binding LoginButtonText}"
                        Command="{Binding LoginCommand}"
                        Style="{StaticResource RoundedButton}"
                        Margin="10,0,0,0" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>