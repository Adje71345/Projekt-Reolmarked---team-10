<UserControl x:Class="Reolmarked.View.RackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Reolmarked;component/Resources/RackPanelsViewLocators.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <sys:Double x:Key="DesignWidth">1200</sys:Double>
            <sys:Double x:Key="DesignHeight">520</sys:Double>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="210" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
            CornerRadius="36"
            Background="#FFFFFF"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            Padding="20">
            <Viewbox Stretch="Uniform" StretchDirection="Both">
                <Grid Width="{StaticResource DesignWidth}" Height="{StaticResource DesignHeight}">
                    <ItemsControl ItemsSource="{Binding RackSlots}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                <Setter Property="Canvas.Top"  Value="{Binding Y}"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.SelectRackByIdCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding RackId}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Width"  Value="28"/>
                                            <Setter Property="Height" Value="44"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="Box"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    Background="#E8F5E9"
                                    BorderBrush="#81C784">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                                <Setter TargetName="Box" Property="Background" Value="#EEEEEE"/>
                                                                <Setter TargetName="Box" Property="BorderBrush" Value="#BDBDBD"/>
                                                            </DataTrigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Box" Property="BorderBrush" Value="#1565C0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>

                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHorizontal}" Value="True">
                                                    <Setter Property="Width"  Value="44"/>
                                                    <Setter Property="Height" Value="28"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <TextBlock Text="{Binding RackId}" FontWeight="SemiBold"/>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Viewbox>
        </Border>

        <Separator Grid.Column="1" VerticalAlignment="Stretch"/>

        <ContentControl Grid.Column="2" Content="{Binding CurrentRackPanel}"/>
    </Grid>
</UserControl>
